{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "ci:publish": "node scripts/resolve-workspace-protocols.mjs && bunx changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8"
  }
}
